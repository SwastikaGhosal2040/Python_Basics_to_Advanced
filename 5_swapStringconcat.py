a = input('Enter value of a =')
b = input('Enter value of b =')
temp = a
a = b 
b = temp
# using string concatenation
# we an concatenate 'After swapping a = ' and a because both contains a string
# a = input()....we know input function returns sring
# so a contains string
#part1 contains string
# so we can concatenate these two
print('After swapping a = ' + a)
print('After swapping b = ' + b)